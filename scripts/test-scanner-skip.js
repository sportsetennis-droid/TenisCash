const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const s=fs.readFileSync('public/bipar.html','utf8');const body=s.split('window.pularEtiquetaIlegivel = function () {')[1].split('\n  };')[0];let saved=0,reset=0;const item={lid:'one',b64:'photo',st:'pendente'};const ctx={etiqFila:[item],etiqAtivo:'one',etiqBusy:false,etiqSave:()=>saved++,clearInterval:()=>{},etiqWatch:1,etiqRetryAt:0,etiqUltimo:'x',etiqUltimoTs:1,etiqPausado:true,window:{ScannerAuto:{stopDecoder:()=>{}}},etiqAuto:{reset:()=>reset++},document:{getElementById:id=>id==='etiq-video'?{play:()=>Promise.resolve()}:{style:{}}},renderEtiqLista:()=>{},Date};vm.createContext(ctx);vm.runInContext('(function(){'+body+'})()',ctx);assert.equal(item.st,'revisao');assert.equal(item.b64,'photo');assert.equal(ctx.etiqAtivo,null);assert.equal(reset,1);assert.equal(saved,1);for(const guard of [{ean:'123'},{capId:'server'},{complete:true}]){Object.assign(item,{ean:null,capId:null,complete:false},guard);ctx.etiqAtivo='one';vm.runInContext('(function(){'+body+'})()',ctx);assert.equal(ctx.etiqAtivo,'one');}console.log('PASS skip keeps photo, releases camera, never skips a counted/identified capture');
